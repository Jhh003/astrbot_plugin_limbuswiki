# Core dependencies (no external packages required for basic functionality)
# The plugin uses only Python standard library for core features

# Optional dependencies for WebUI (install if you want to use WebUI management)
fastapi>=0.100.0
uvicorn>=0.22.0
python-multipart>=0.0.6
